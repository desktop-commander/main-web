{
  "name": "main-web",
  "version": "1.0.0",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "build": "cd ./root && npm run build && cd ../prompt-library && npm run build",
    "build:dev": "cd ./root && npx vite build --mode development && cd ../prompt-library && npx vite build --mode development",
    "postbuild": "echo \"desktopcommander.app\" > docs/CNAME",
    "commit-static": "git rm -r --cached docs/ 2>/dev/null || true && npm run build && git add docs/ && git commit -m 'Static website changes'",
    "serve": "npx serve docs --listen 3000",
    "serve:static": "npx serve docs --listen 3000",
    "serve:docs": "npx serve docs --listen 3000"
  },
  "repository": {
    "type": "git",
    "url": "git@gh.dc.app:desktop-commander/main-web.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@nx/vite": "^21.5.2",
    "@types/node": "^24.5.2",
    "http-server": "^14.1.1",
    "nx": "21.5.1",
    "serve": "^14.2.3"
  },
  "nx": {},
  "workspaces": [
    "prompt-library",
    "root"
  ],
  "dependencies": {
    "vite": "^7.1.6"
  }
}
